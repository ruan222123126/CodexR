{
  "name": "codexsidebar",
  "displayName": "codexSidebar",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.108.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:codex.chatView",
    "onCommand:codexSidebar.restoreLatestCheckpoint",
    "onCommand:codexSidebar.restoreCheckpoint"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "codex-sidebar",
          "title": "CodexR",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "codex-sidebar": [
        {
          "type": "webview",
          "id": "codex.chatView",
          "name": "Chat with AI"
        }
      ]
    },
    "commands": [
      {
        "command": "codexSidebar.restoreLatestCheckpoint",
        "title": "CodexR: Restore Latest Checkpoint (Temporarily Unavailable)"
      },
      {
        "command": "codexSidebar.restoreCheckpoint",
        "title": "CodexR: Restore Checkpoint... (Temporarily Unavailable)"
      }
    ],
    "configuration": {
      "title": "Codex Sidebar",
      "properties": {
        "codexSidebar.parserMode": {
          "type": "string",
          "enum": [
            "v2",
            "legacy"
          ],
          "default": "v2",
          "description": "Streaming parser mode. Use legacy for quick rollback if parsing regression occurs."
        },
        "codexSidebar.showToolUsageIndicator": {
          "type": "boolean",
          "default": true,
          "description": "Show tool usage count in the Thinking Process summary and highlight it when tools were used."
        },
        "codexSidebar.codexThinkingNoiseFilterEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Filter low-value Codex thinking lines (e.g. Planning/Preparing headings) before rendering."
        },
        "codexSidebar.codexHideThinking": {
          "type": "boolean",
          "default": false,
          "description": "Hide thinking process output for Codex. When enabled, only the final answer will be shown."
        },
        "codexSidebar.codexAutoResumeSession": {
          "type": "boolean",
          "default": true,
          "description": "Automatically resume the previous Codex exec session for follow-up requests."
        },
        "codexSidebar.claudeAutoResumeSession": {
          "type": "boolean",
          "default": true,
          "description": "Automatically resume the previous Claude session for follow-up requests."
        },
        "codexSidebar.claudeDisableThinking": {
          "type": "boolean",
          "default": false,
          "description": "Disable thinking process output for Claude. When enabled, Claude will not show its reasoning process."
        },
        "codexSidebar.piAutoResumeSession": {
          "type": "boolean",
          "default": true,
          "description": "Automatically resume the previous Pi session for follow-up requests."
        },
        "codexSidebar.piDisableThinking": {
          "type": "boolean",
          "default": false,
          "description": "Disable thinking process output for Pi. When enabled, Pi will not show its reasoning process."
        },
        "codexSidebar.codexEnforceCheckpointPolicy": {
          "type": "boolean",
          "default": true,
          "description": "Inject a system policy asking Codex to create a checkpoint before file edits."
        },
        "codexSidebar.codexRequireHardCheckpoint": {
          "type": "boolean",
          "default": true,
          "description": "Create a real workspace snapshot checkpoint before every Codex request and block execution when checkpoint creation fails."
        },
        "codexSidebar.codexHardCheckpointRetention": {
          "type": "number",
          "default": 20,
          "minimum": 1,
          "description": "Maximum number of hard checkpoint snapshots kept in extension global storage."
        },
        "codexSidebar.titleGenerationMode": {
          "type": "string",
          "enum": [
            "currentProvider",
            "fixedProvider",
            "firstMessage"
          ],
          "enumDescriptions": [
            "Use the current active provider (Codex/Claude/Pi) to generate session titles",
            "Always use a fixed provider to generate session titles",
            "Use the first user message as the session title"
          ],
          "default": "currentProvider",
          "description": "How to generate session titles automatically."
        },
        "codexSidebar.titleFixedProvider": {
          "type": "string",
          "enum": [
            "codex",
            "claude",
            "pi"
          ],
          "default": "codex",
          "description": "The fixed provider to use for title generation when titleGenerationMode is set to 'fixedProvider'."
        },
        "codexSidebar.language": {
          "type": "string",
          "enum": [
            "en",
            "zh-CN"
          ],
          "default": "en",
          "description": "Display language for the interface"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "security:check": "npm audit --audit-level=high",
    "check:strict": "npm run lint && npm run compile && npm run test && npm run security:check"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.108.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.39.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0"
  },
  "dependencies": {
    "dompurify": "^3.2.6",
    "marked": "^15.0.7"
  }
}
